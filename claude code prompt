请基于以下背景与要求，帮助我设计一个 Creator Commerce System 的整体系统架构、模块边界、数据模型与实施方案。
目标不是只做一个 dashboard，而是构建一个可扩展的 creator 商业化系统。
请你从 系统架构、模块拆分、数据库 schema 草图、核心流程、接口设计建议、前后端边界、未来扩展性 等角度输出。

1. 项目背景

我们公司正在开发自己的 ERP，用于统一管理所有产品、订单、客户、库存、履约、财务和数据。

与此同时，我们要开发一个面向设计师/creator 的系统。这个 creator 系统的核心目标是：

允许 creator 上传自己的作品
允许 creator 选择产品模板，例如衣服、杯子、帽子、帆布袋等
系统根据作品和产品模板生成 preview / mockup
creator 可以进入设计器单独编辑，例如调整位置、缩放、旋转、适配印刷区域
creator 满意后，可以决定是否发布销售
销售渠道支持两类：
在我们自己的 Shopify 平台上卖
同步到 creator 自己的店铺卖
creator 可以二选一，也可以两个渠道同时卖
creator 可以分别设置两个渠道的价格，例如：
在我们平台卖 $29
在 creator 自己的 store 卖 $35
creator 未来需要看到自己的订单、收入、设计表现、渠道数据等

我们现在已经有一个正在开发中的“设计器（design editor）”，它不是完全开发完成，但可以继续调整。
这个设计器当初的定位就是未来可复用，不只是给 creator 系统用，后面也可能给其他系统用。

因此我们现在需要思考：

ERP 应该扮演什么角色
Creator System 应该怎么建
Design Editor 应该怎么接入
未来如何支持多个前端系统共同调用设计器和 ERP 数据
2. 我们当前想要的系统边界

我目前倾向的方向不是把 creator 功能直接塞进 ERP，也不是完全另起一套孤立系统。

更理想的方向是三层结构：

Layer 1: ERP Core

ERP 作为底层真实数据源（source of truth），负责管理：

产品
SKU
库存
订单
履约
发货
客户
财务结算底账
payout ledger
settlement ledger

ERP 的目标是：

数据准确
可审计
内部管理
统一业务真相
Layer 2: Design Engine

设计器不要只是某个页面功能，而是作为一个独立的可复用能力层。
它的职责包括：

design asset 管理
设计编辑 session
产品模板适配
图层配置
印刷区域 / safe zone 管理
preview / mockup 生成
print file 生成
保存可编辑配置

它未来除了给 creator 系统使用，也可能给：

内部运营后台
商家端系统
其他业务系统
Layer 3: Creator Portal / Creator Commerce App

这是给 creator 使用的前端系统，负责：

creator onboarding
上传作品
内容管理
选择产品
调用 design editor 编辑
选择销售渠道
设置不同渠道价格
查看订单
查看 earnings / payouts
查看设计表现
连接自己的 store

Creator Portal 应该是单独设计的前端体验，而不是 ERP 风格后台。

3. 需要重点支持的商业模式

creator 系统必须支持两种模式，并允许同时存在：

Mode A: Creator 在我们平台卖
creator 上传作品
creator 选择产品
creator 发布到我们自己的 Shopify 平台
我们负责流量、运营、listing、履约
creator 收到 royalty / revenue share
Mode B: Creator 在自己的 store 卖
creator 有自己的 Shopify store（未来也可能是 TikTok Shop、Etsy 等）
creator 用我们的设计工具、产品模板、供应链和履约系统
creator 将生成的产品同步到自己的 store
订单进入后由我们履约
creator 获取产品利润，我们获取供应链利润
Hybrid Mode
同一个 creator
同一个 design
同一个产品实例
可以同时在我们平台卖，也可以同步到 creator 自己的 store
两边价格可以不同
两边收入模型不同
系统必须支持这种多渠道分发逻辑
4. 设计器在系统中的角色

当前设计器不是完全开发完，但已经在做了。
它应该被设计成一个 可复用的 Design Engine / Design Service，而不是被写死在 creator 系统里。

我的倾向是：

短期采用嵌入式或可调用方式
Creator Portal 调用 Design Engine
Design Engine 独立维护
长期保留给其他系统复用的可能

请你在输出方案时重点思考：

Design Engine 和 Creator Portal 的最佳边界
Design Engine 适合以什么方式接入（例如 iframe / embedded app / micro-frontend / 独立 service）
如何保证用户体验看起来是一体的
如何设计可扩展的接口，让其他系统未来也能调用这个 Design Engine
5. Creator Portal 需要具备的主要功能
5.1 Dashboard

creator 登录后看到自己的总览：

今日收入
本月收入
累计收入
待结算金额
已结算金额
top selling designs
top products
渠道表现（marketplace vs own store）
5.2 Design / Content Management

creator 可以上传和管理自己的作品：

上传原始 artwork
上传 preview image
上传 source file（可选）
编辑标题、描述、标签、分类
管理草稿 / 待审核 / 已发布 / 已下架 / 已归档
查看每个 design 的表现
5.3 Product Builder

creator 基于一个 design 选择产品模板：

t-shirt
mug
hat
tote bag
future templates

系统生成 mockup/preview 后，creator 可以点击进入设计器进行微调。

5.4 Design Editor Integration

creator 在 Product Builder 中点击某个产品实例后，可以进入设计器：

调整 artwork 位置
缩放
旋转
适配 print area
生成 preview
保存 product configuration
返回 Product Builder
5.5 Channel Distribution

creator 决定每个 product instance 的销售渠道：

发布到我们自己的 marketplace
同步到 creator 自己的 store
两者都选

并且可以分别设置：

marketplace price
own store price
5.6 Orders / Sales / Earnings

creator 可以看到：

按渠道查看订单
按 design 查看订单
按 product 查看销量
收入来源
payout 状态
royalties / margins
5.7 Store Connection

creator 可以连接自己的 store，至少第一阶段考虑 Shopify：

connect Shopify
sync products
sync listing data
未来可能读取订单回传
管理同步状态
6. 我希望你重点思考的核心数据对象

请围绕以下对象进行数据库设计建议和关系说明：

ERP Core Objects
products
product_templates
skus
inventory
orders
order_items
shipments
customers
payout_ledger
settlement_ledger
Creator Objects
creators
creator_profiles
creator_channels
creator_store_connections
creator_payouts
creator_earnings
Design Objects
designs
design_versions
design_assets
design_tags
design_status
Design Engine / Product Config Objects
editor_sessions
product_templates
print_areas
product_configurations
preview_assets
print_assets
sellable_product_instances
Channel / Commerce Objects
channel_listings
channel_prices
listing_status
sync_jobs
publishing_records

请明确区分：

Design（创作资产）
Product Template（产品模板）
Product Configuration / Product Instance（设计应用到具体产品后的可售实体）
Channel Listing（某个可售实体在某个渠道中的销售实体）

这个区分对我们很重要。

7. 需要你输出的内容

请你输出一个完整方案，至少包含以下内容：

A. 系统总体架构

请用清晰方式描述 ERP Core、Design Engine、Creator Portal 三层架构，以及它们之间的数据流和职责边界。

B. 模块边界说明

请明确哪些功能属于 ERP，哪些属于 Design Engine，哪些属于 Creator Portal。

C. 数据模型建议

请提供一个数据库 schema 草图或实体关系设计建议，至少给出核心表、关键字段、外键关系。

D. 核心流程设计

请描述以下流程：

creator 上传 design
creator 选择产品模板
creator 进入 design editor 编辑
保存 product configuration
creator 选择 channel
creator 设置不同渠道价格
发布到 marketplace / 同步到 own store
订单产生后如何回流到 ERP
如何计算 creator earnings / royalties / payouts
E. Design Engine 接入方案建议

请比较几种接入方式，例如：

iframe embedded
micro-frontend
standalone app + token
internal module

并给出短期最合理方案、长期演化路线。

F. API / Service 边界建议

请建议 Creator Portal 与 Design Engine、ERP 之间需要有哪些 API 或 service interface。

例如：

create editor session
get design config
save config
generate preview
create channel listing
publish to marketplace
sync to store
get creator earnings
get creator order summaries
G. 版本路线图

请给出一个现实的分阶段实施建议：

Phase 1: 最小可运行版本
Phase 2: 渠道与同步增强
Phase 3: creator 生态化
8. 重要设计原则

请在方案中遵守以下原则：

ERP 是 source of truth，不要让 creator 系统复制一整套 ERP 真相
Creator Portal 是外部产品体验层，不要做成 ERP 风格
Design Engine 要可复用，不要写死在 creator 系统里
系统必须支持 marketplace、own store、hybrid 三种模式
设计资产、产品配置、渠道 listing 必须严格区分
数据结构从一开始就要考虑扩展性，避免后期重构
第一版不要过度复杂，但架构边界要清楚
优先保证未来可以支持更多产品模板、更多渠道、更多 creator
9. 输出风格要求

请用工程和产品都能看懂的方式输出。
不要只给抽象概念，要尽量落地。
最好能包含：

架构图的文字版说明
模块清单
数据表建议
关键字段建议
流程说明
技术建议
风险提醒

如果你认为有必要，也可以在最后补充：

当前最危险的架构误区
我们现在最应该优先做的 3 件事
哪些地方短期先简化，哪些地方必须从 Day 1 设计对